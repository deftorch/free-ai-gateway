{
  "name": "@free-ai-gateway/server",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "bun --watch --env-file=../../.env src/serve.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@free-ai-gateway/adapter-gemini": "workspace:*",
    "@free-ai-gateway/adapter-nvidia-nim": "workspace:*",
    "@free-ai-gateway/core": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "typescript": "^5.6.0"
  }
}
